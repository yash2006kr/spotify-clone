export default function BackendHome() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 32 }}>
      <h1>Spotify Clone API</h1>
      <p>The backend is running. Use the /api routes from the frontend.</p>
    </main>
  );
}
